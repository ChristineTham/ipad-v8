# 2026-08-20 — the netfs fault, measured instead of inferred

K15 ended yesterday at 6/12 with a build that died on

	"/n/tree/src/history/ix/src/jerq/mux/32ld.c":214:syntax error
	"/n/tree/src/history/ix/src/jerq/mux/32ld.c":214:saw EOF

over a **295-line** file. Today's work was not on mux at all: it was on the
three claims I had attached to that failure, two of which were wrong, and on
building instruments so the next reading cannot be argued about.

---

## 1. The guest is NOT starved, and the number that said otherwise was mine

`tools/v10-clock.sh` — fixed local guest work (`cat /unix > /dev/null`,
228,310 bytes off local disk), timed by the **host**, card attached and then
disabled, three reps, median. No network in either arm.

	il off    46, 44, 44 ms      median 44
	il on     69, 193, 72 ms     median 72
	                              ratio 1.64x

**The harness refused to draw a conclusion**, and its refusal was correct
policy even though the effect is probably real: 1.64× lands in the band it
declines to interpret, and 44 ms of work is close to the tty round trip that
measures it. What the reading *does* settle is the question it was built for —
**a guest that reads 228 KB in 72 ms is not too starved to issue netfs
requests.** Nothing about a 1.64× tax can produce a multi-second request.

The `off` arm is tight to ±2 ms and the `on` arm is 69/72 with one outlier, so
there is very likely a real ~64% cost to having the card attached. That is worth
knowing and is not today's bug. Raising the workload is the way to settle it,
which is what the harness already tells you to do.

---

## 2. "~9.6 s per request" was a request RATE, not a service time

This is the correction that mattered most, because every other reading was being
interpreted through it.

netfsd's trace **has no timestamps** — I checked, rather than remembering. So
the figure could only have come from one place: the run's elapsed time divided by
`connection closed after 399 requests`. That is a request *rate*, and a guest
that spends most of its time compiling produces exactly that number without the
server being slow at all. The units read as latency because of the words "per
request".

CLAUDE.md already records this error twice — the withdrawn "530 file reads in
2 s", and `used = s_fsize - s_tfree` counting the i-list as content. Same shape:
**a quotient whose divisor does not mean what the units imply.**

### The instrument, so it cannot recur

`netfs/Sources/NetFS/Server.swift`'s connection loop already had the boundary in
it — `wire.readExactly()` *blocks* until the guest asks, so time inside it is the
guest's and time inside `serve()` is ours. Two monotonic counters (`DispatchTime`,
not `Date`, because this process runs for hours beside a guest that thinks it is
1976) now split them, and the closing line reports:

	connection closed after N requests  (serving X ms total, Y ms mean,
	                                    Z ms worst; waiting on the guest W s)

### And the honest replacement figure

Measured today on a workload with **essentially no guest computation** — `wc -c`
and `cat` over a share, nothing compiled — by sampling netfsd's request count
across a 90-second window mid-run:

	12 requests in 90 s   =   7.50 s per request

Same order as the K15 figure, but now on a workload where the divisor means what
the units say. **So netfs really is seconds-per-request slow; the K15 number just
could not have proved it.**

---

## 3. The truncation is real, and it is EXACTLY one block

Host-side, no simulator, one command:

	32ld.c is 5870 bytes, 295 lines
	head -c 4096 | wc -l   ->   213

and cpp reported the error at line **214**. netfsd served one
`NREAD count=4096 off=0` and was never asked again. So cpp saw exactly the first
block and took it for the whole file.

**Three 4096-byte replies in the same run were served perfectly**, which retracts
the frame-size hypothesis I was carrying:

	mpx.h      4096 @ 0,  804 @ 4096, 0 @ 4900    complete
	pads.h     4096 @ 0, 1247 @ 4096, 0 @ 5343    complete
	filehdr.h  4096 @ 0, 1134 @ 4096, 0 @ 5230    complete
	32ld.c     4096 @ 0   -- and nothing, ever    TRUNCATED

A full-size reply is not what breaks. The failing read also never sent `NPUT`
for its tag, where every completed read does — the shape of an abandoned handle.

`naread()` (`lsys/fs/neta.c:222`) is a clean read-until-short loop that never
consults `i_size`, so it behaved correctly: the caller asked for 4096, got 4096,
`u_count` hit zero, loop ended. **The fault is above it — in a second `read(2)`
that never reached the wire.**

---

## 4. The probe: two readers, because one cannot localise it

`tools/v10-netread.sh` + `.exp`. No compiler, no build, no libraries — `wc -c`
against the host's own `stat` is the whole measurement, which is why it costs one
boot instead of the fifty minutes K15 spent to find this once. Every file is read
three times, because K14 read a 5106-byte file correctly on the same transport
and a single clean pass proves nothing.

**Two readers, and the split is the point:**

	wc   NBUF = 64*1024  (cmd/wc.c:3)     ONE read(2); naread()'s own loop works
	cat  BLOCK = 4096    (cmd/cat.c:8)    REPEATED read(2) -- cpp's exact pattern

cpp's `refill()` does `read(fileno(fin), pbuf, BUFSIZ)` and r70's `stdio.h` makes
`BUFSIZ` 4096, so `cat file > local` reproduces the failing pattern precisely,
and `wc` tests the other path. A probe with only `wc` could have passed while the
bug stood.

### Two traps caught host-side, before spending a boot

- **`wc -c` prints `%7ld`.** `printout()` right-aligns in seven columns, so
  `` n=`wc -c < f` `` yields `"   5870"` and a quoted `test "$n" = 5870` is false
  for **every** file. The probe would have reported total truncation over a
  working transport. Unquoted, sh's field splitting discards the padding.
- **`show il stats` has never worked.** pdp11_il.c's MTAB row names
  `"STATISTICS"` and SIMH matches in full, so the command answered
  `%SIM-ERROR: Non-existent parameter: STATS` — which was not one of
  `v10_ilstats`'s patterns either, so it fell through to a 30-second timeout and
  the reading was silently absent from every V10 halt since it was added. Fixed,
  and the error text is now matched explicitly so a future rename is loud rather
  than slow.

---

## 5. A mechanism from Bell Labs, in their own words

`ilincmd()` waits for a command-completion interrupt with
`tsleep((caddr_t)is, PZERO, 5)` and `slp.c:86` declares that third argument
`seconds` — a **5-second** timeout, tantalisingly close to the measured 7.5 s.
**It is not the cause**: its only callers are lines 180/195/207, all
`ILC_RESET`/`ILC_STAT`/`ILC_ONLINE`, i.e. init. Recorded because I believed it
for several minutes.

The hot path carries something better sourced. `lsys/io/ni1010a.c`:

	/*
	 * add receive buffer space if needed
	 * -- let it be an ordinary command, interrupt when complete, for now.
	 * this is probably too slow
	 */
	ilrcvbufs(is)
	{
		if (is->flags & CMDACT)
			return (0);

**Bell Labs flagged this path as slow themselves.** `ilrcvbufs()` posts *one*
buffer per command and refuses to post while any command is active; it wants
`ILRBYTES` = `ETHERMAXTU*2` = 3000 bytes in `ILRSIZE` = 1024-byte blocks, so
three buffers, each needing its own command and its own interrupt round trip.

**V8's `ill.c` chains instead** — `bp->next = allocb(n)`, with the next buffer
supplied from *inside* `ilrint()` — so it never pays that round trip. Same card,
same device model, different driver. That is the right shape to explain 7.5 s
against the ~16 ms per request `pdp11_il.c:775` records for *unpatched* V8, a
**470×** gap on identical simulated hardware.

It is a mechanism and not yet a measurement. `show il statistics` (drops, now
that the command name works) and netfsd's serving-versus-waiting split are what
decide it, and both arrive with the probe's first complete run.

---

## Standing corrections from today

| claim | status |
|---|---|
| "netfsd serves the V10 guest at ~9.6 s per request" | **withdrawn** — a rate, not a service time. 7.50 s/request is the measured replacement, on a workload with no guest computation |
| "the guest is slow at network-free work / may be starved" | **withdrawn** — 228 KB in 72 ms |
| "a 4096-byte reply is roughly three frames and one is dropped" | **withdrawn for this fault** — three 4096-byte replies succeeded in the same run |
| "`show il stats` reads the drop counters at every V10 halt" | **was never true** — wrong parameter name; fixed today |

---

## 6. The mechanism was wrong too — and the right fix is the PATH, not a copy

`ilrcvbufs()`'s *"this is probably too slow"* is Bell Labs describing real
hardware, and it does not describe our emulation of it. Our model's dispatch tail
calls `il_setcint()`, which does `SET_INT (ILC)` **immediately**, so a command
completes before the guest's CSR write returns. Posting a receive buffer is
therefore free here, and that hypothesis is dead — the third of the day.

**What actually dominates is measured, and it is the path walk.** Two accesses to
one 1905-byte source over a share mounted at the tarball root:

	9 NGET + 9 NNAMI + 10 NPUT      28 requests of path walking
	6 NREAD                          6 requests of actual data
	1 NSTAT

**80% of the traffic spent finding an eight-component path.** CLAUDE.md has said
"netfs costs a round trip per path component" since the N track; what is new is
how completely it dwarfs everything else, and what follows from it:

> **Copying the sources to local disk first does not help, because the copy pays
> the identical walk.** Task #16 had "stage source locally" written down as the
> fix and it would have bought nothing.

Shortening the path does. So `mux.mk` now names its source directory through
`$(MUXSRC)` instead of inlining `$(SRC)/history/ix/src/jerq/mux` into fourteen
rules, `v10-mux.sh` serves that directory as a **fourth share**, and the harness
mounts it at `/n/mux` on device 3 — `/n/mux/32ld.c`, one component instead of
seven. `$MUXD` follows it, which also shortens the oracle's `ar x lib.a`.

`SRC` deliberately stays the tarball root: `IXLIBC` still reads `labEQ.c` and
`labLE.c` through it, and two files do not justify a fifth mount.

### And the oracle archive is now length-checked before it is trusted

`lib.a` is 59,822 bytes — **fifteen blocks** — read over the very transport that
truncates. The guard was `ar x lib.a && test -s mux.o`, and CLAUDE.md's own rule
says why that is not enough: `test -s` cannot tell a truncated member from a whole
one. A short extract would have handed the oracle a corrupted symbol table and the
124-byte `_buf` answer would have come back wrong with nothing saying so. The
length is asserted first, the extract is gated on it, and the expected size is
measured **host-side by the driver and passed in** — a constant that restates a
measurement drifts away from it silently.

---

## 7. Two units corrections, one of them in our own patch

`slp.c` states it above `tsleep()` itself:

	 * Return in no more than the indicated number of seconds.
	 * (If seconds==0, no timeout implied)

So the third argument is **seconds**, and three readings follow:

- `istread()`'s `tsleep(…, PRIBIO, 30)` is a **thirty-second** wait. Our own
  overlay comment called it "30 ticks" — corrected. Thirty seconds is the right
  order for the bare `I/O error`s K15 saw, where "30 ticks" suggested something
  negligible.
- `stread()`'s `tsleep(…, STIPRI, 0)` really is **forever**, which is what
  CLAUDE.md already says about the QDELIM trap.
- `ilincmd()`'s `tsleep(…, 5)` really is **five seconds** — but only on the init
  path (`ILC_RESET`/`ILC_STAT`/`ILC_ONLINE`), so it is not the latency however
  well 5 fits 7.5.

### A worry of my own, settled rather than left hanging

Could the ~7 s per request be the *harness* typing on the console rather than
netfs? No, and the clock probe already answered it: a whole `v10_sh` round trip
**including 228 KB of local disk I/O** measured **44 ms**. The V10 harness drives
the console rather than the DZ, so the `Speed=*32` factor never applied and never
needed to.

---

## 8. Rung 8's port is landed

`muxix.c` is a **`NEWFILES` entry in `tools/v10-overlay.py`** — generated,
documented in `PATCHES.md`, covered by `--check` — rather than a fifth
hand-maintained addition of the kind a prune once deleted. `labEQ.c`/`labLE.c` are
Bell Labs' own IX sources compiled unchanged, named in `mux.mk`'s OBJS.

**Yesterday's reasoning was wrong twice and the code accidentally right**, which
is worth recording as plainly as the result:

- `mux.c:1051`'s `unsafe()` is **not** the event loop. It is inside
  `checklabs()`, which passes all-ones fd sets to ask what is readable *now* and
  loops `while(nseen)` only while it keeps finding some.
- `select`'s fourth argument is **milliseconds**, and `sys2.c:242` short-circuits
  on zero — so `0` means **poll**, not block. A draft comment calling it "the
  blocking timeout, which is what the event loop wants" was wrong in both
  directions.

And better than either: **none of the three can be reached on V10.**
`checklabs()` is armed only by `SIGLAB`; r70's `signal.h:37` defines it as
`26 /* … secure unix only */`; and `SIGLAB` appears **nowhere in `lsys/`**, so the
kernel never raises it. `NSIG` is 32, so the `signal(2)` call is accepted and
never fires. They have to link, not to work — which is the honest reason `unsafe`
is three lines.

---

## 9. THE ANSWER: the fourth Datakit assumption, and V8 already fixed it

`tools/v10-netread.sh` reproduced the fault, and the reply-size distribution is
the whole argument:

	 744 bytes  x6     complete
	1905 bytes  x6     complete
	4096 bytes  x1     <- the connection died on it
	   0 bytes  x24    post-mortem, silently

Twelve replies under one block read correctly — both readers, three passes each.
**The first reply that filled a whole 4096-byte request killed the connection**,
and afterwards every read returned **0 bytes**: EOF, not an error. That is why 20
verdicts flew past with the request count frozen at 249.

Request #249 was `NREAD tag=70 ino=35 count=4096 off=0` on `mpx.h` — served 4096
bytes, then nothing. K15 died on exactly that shape (`32ld.c`, `NREAD 4096@0`).
And `mpx.h` is one of the three files that read *correctly* in K15, so the trigger
is intermittent on identical input.

### The two numbers

	V8    struct qinit strdata = { strput, NULL, nulldev, nulldev, 8192, 4096 };
	V10   struct qinit strdata = { strput, NULL, nilopen, nulldev,  512,  256 };

**The N track raised V8's stream head sixteenfold as part of the netfs fix, and
V10's copy was never touched.** `tcp_device.c:265` drains only
`while ((q->next->flag&QFULL) == 0)`, so every netfs reply squeezes through a
512-byte queue. Our V10 overlay had fixed *two* of `streamio.c`'s Datakit
assumptions — the freed block tail and the zero-length read — and left this one.

CLAUDE.md has described this exact hazard since the N track: *"sits behind a
stream head only **512 bytes** wide (`strdata`; `q->count` is bytes, and
`tcpdisrv` only drains `while((q->next->flag&QFULL) == 0)`)"*. It was written
about V8 and it is equally true of V10; nobody had checked.

### Why this is a diagnosis and not a guess

The three instruments built today each exclude a suspect:

	netfsd     serving 0.31 ms mean, 3.9 ms worst; waiting on the guest 1550.2 s
	the card   receive errors 0, transmit errors 0, Read Queue Loss 0
	the guest  228,310 bytes read locally in 72 ms

Not the server. Not a dropped frame. Not starvation. And the card's counters could
only be read at all because the `STATS` → `STATISTICS` fix landed this morning.

### A retracted hypothesis returns, correctly scoped

Earlier today I withdrew "a 4096-byte reply is roughly three frames and one is
dropped" on the strength of three such replies succeeding in K15. The withdrawal
of *that mechanism* stands — drops are zero. But the **trigger** really is a
full-block reply, and the distinction matters: not "a 4096-byte reply loses a
frame" but "**a reply that fills the request** overruns a 512-byte queue".

### The patch

One line, V8's own values, in the overlay with its reasoning in `PATCHES.md`. The
authenticity rule permits it precisely here: the tape cannot run as-is, and
`usr/src/netfs/README` asked for it in advance — *"The code here assumes it is
talking to Datakit in several places. If you want to use another network, you'll
have to fix things."*

**Not yet verified.** `streamio.c` is kernel source, so the chain is
`v10-srcdisk.sh` → `v10-kernel.sh` (K7) → `v10-netboot.sh` (K13) → re-run
`v10-netread.sh`. Derived images cloned to `*.pre-strdata` first.

---

## 10. The fix is verified into the kernel — and it does NOT fix the latency

The chain ran clean, one simulator at a time:

	tools/v10-srcdisk.sh                                  52/52  exit 0
	tools/v10-kernel.sh ...stage1.k102                    20/20  exit 0
	tools/v10-netboot.sh ...stage1.k102.k7                33/33  exit 0

K7's own assertions prove the patch reached the kernel rather than merely the
repo — *"our streamio.x compiles and assembles"* and *"streamio.x replaces the
tape's in os.a"* — and `v10/mk/gen/kobj.order` names
`lsys/os/streamio.c → streamio.x → os.a`, generated from the overlay by
`kernel_objects()` so it cannot go stale. That mattered: CLAUDE.md records
`kobj.order` going on naming a **withdrawn** patch, and the same mechanism would
silently *omit* a new one.

### A correction, caught before it was written down as a result

netfsd's log showed `connection closed after 77388 requests (… 0.21 ms mean;
waiting on the guest 1278.8 s)` — 16.5 ms a request, and for a moment that read
as a 375× win. **It is `netfs-v10stree.log`, the source-disk run, and that runs
on V8**, which has had the fix since the N track. So it is a V8 baseline, and a
welcome one: it lands almost exactly on the ~16 ms per request
`pdp11_il.c:775` records. It says nothing about V10.

Nor can K13's own `netfs-nb*` logs answer it — dividing their `waiting` by their
request count would repeat this morning's exact error, since K13 spends most of
its time compiling locally. **Only the controlled probe can, because its workload
is nothing but reads.**

### The controlled answer: unchanged

	before the fix   6.67 - 7.50 s per request
	after the fix    6.92 s per request

**So the 512-byte stream head was not the latency**, and #13 stays open. What the
fix may still address is the *death* — the two are separate faults that arrived
together, and it was worth not assuming one patch cured both.

### One piece of positive evidence, short of the controlled test

K13's own verification read `v10/src/lsys/os/streamio.c` whole over netfs to check
for the `ipnx: satisfied` marker, and found it. That file is **23,024 bytes — six
blocks** — where before the fix a *two*-block file (`mpx.h`, 4900) killed the
connection at its first full-block reply. Suggestive, not conclusive: K13 passed
before the fix too.

### Where #13 stands, with one candidate narrowed and one ruled out

The guest sent **978 packets and received 833** for 249 exchanges, with zero drops
reported anywhere — more sends than receives, which points at retransmission.
V10's TCP timers make that expensive:

	                  V8                 V10
	PR_SLOWHZ         1  tick/second     1  tick/second      <- THE SAME
	TCPTV_MIN         1  second          2  seconds
	TCPTV_MAX        30  seconds       120  seconds

**Timer granularity is ruled out**: both editions tick once a second, and V8 does
~16 ms per request with it, so V8's RTO plainly never fires. V10's minimum RTO
being 2 s only sets the *penalty* — 2 s, then 4, then 8 under backoff, which is
the right shape for a 7 s average. It does not explain why a retransmit happens
at all on a loopback with no loss.

**The instrument that would decide it is a packet trace** — `set il debug=DAT`
dumps every frame, so a duplicate is visible directly. That is the next step for
#13, and it is a performance question rather than a correctness one, so it is not
what blocks rung 8.

### VERIFIED: the second read now happens

The controlled comparison could hardly be tighter — same probe, same file, same
tag, same request number, one variable changed:

	                        before the fix          after the fix
	#249 NREAD 4096 off=0   4096 bytes served       4096 bytes served
	#250                    CONNECTION CLOSED       NREAD 4096 off=4096

**The guest issued the second read that it had never issued before.** That is the
whole of the K15 failure — `cpp` asked for a block, got it, and its next `read(2)`
never reached the wire — and it is gone.

So the two faults really were separate, and only one of them was the stream head:

	the truncation and the connection death   FIXED by 512/256 -> 8192/4096
	~7 s per request                          UNCHANGED (6.92 vs 6.67-7.50)

### And 32ld.c itself — the file K15 died on — now reads whole

	#342 NREAD tag=92 ino=110 count=4096 off=0     -> 4096 bytes of 32ld.c
	#343 NREAD tag=92 ino=110 count=4096 off=4096  -> 1774 bytes of 32ld.c

**4096 + 1774 = 5870.** Those 1774 bytes are exactly what `cpp` never saw when it
reported `"32ld.c":214:syntax error / saw EOF` — byte 4096 falls on line 213, and
the missing tail is the rest of the file.

Three 4096-byte replies for `mpx.h` (three passes, 4096 + 804 each) and this one
for `32ld.c`, with **zero connection closes** where the first such reply used to
be fatal.

### The verification run's own ending, which was a harness bug

The probe hit its 3600 s deadline at **24 of 42 verdicts**, and the result to that
point is unambiguous:

	verdicts            24
	short reads          0
	connection closes    0
	full 4096-byte replies served   12

Twelve full-block replies where the *first* used to be fatal, including `32ld.c`
read whole. So the fix is verified; the run simply could not finish.

**Two faults of my own in the harness, both now fixed.**

- **The probe was mis-sized.** Seven files × 3 passes × 2 readers is 42 verdicts,
  and ~7 s a netfs request makes that ~150 s a verdict — 6300 s against a 3600 s
  bound. *A probe that cannot finish inside its own deadline is badly sized, not
  unlucky.* Four files now, one per interesting block count plus the historical
  casualty, and the bound is 5400.
- **The deadline path left the simulator behind.** `v10_reap` is `close` plus
  `wait -nowait`, and **close only ends the simulator if the simulator is READING
  its console** — a guest wedged in a netfs read is not, so SIMH never saw the
  EOF. `v10_expired` then ran `exit 3` and orphaned it: `vax780` pid 83844,
  **PPID 1**, 97.9% of a core, still holding its image, with no pty left to reach
  it through. Nothing but a signal could stop it, which is exactly what
  CLAUDE.md's *"a harness must terminate itself; needing to kill one is the bug"*
  forbids — **so the bug was in the deadline path.** `v10_expired` now sends ^E
  then `quit` before reaping, bounded and non-fatal, the same fallback `v10_halt`
  already uses.

  Only a **clone** was ever at risk — the clone rule earning its keep a third
  time. The harness had already declared that image damaged, `lsof` confirmed the
  orphan held nothing else, and all three baselines were verified untouched before
  and after. The damaged clone is deleted rather than kept, because keeping it
  invites a boot.

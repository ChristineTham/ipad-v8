# 2026-08-18 — K10.1: the world compiles, 238 of 356, and the number is a floor

## The measurement

`bash tools/v10-compile.sh` — stage 1's passes (`cc -B/usr/s1/lib/ -t02p`), every
surveyed command unit under `cmd/`, compile only:

	units compiled              238
	units that did not          118
	units with no source          0   (expr is .y only, omitted by the generator)

	host survey predicted       318   able to resolve every header
	compiler accepted           238
	predicted blocked, built      1   (`units')

**238 IS A FLOOR AND NOT THE ANSWER**, and the controls are what prove it. `as`,
`cc`, `ld`, `mv` and `cpp` all appear in the failing list, and stage 1 builds all
five successfully on this very machine — `cpp`, `c2` and `ld` are the three it was
written to build, and stage 3 proves the toolchain a fixpoint. So a failure here
is not evidence that V10 cannot compile the file. Three causes, each measured:

- **The overlay is not used.** `mv` fails on `"mv.c":274:ROOTINO undefined` — the
  exact defect `v10/src/cmd/mv.c` was written to patch, recorded in CLAUDE.md
  since 2026-08-16. `worldc.sh` compiles `$SRC/src/cmd/` and never looks in
  `$SRC/ours/`, so every correction we carry is invisible to it: `fsck`, `login`,
  `cc`, `mkbitfs` and `mv`.
- **Generated files are absent.** Eight units want `y.tab.h`, `y.debug` or
  `tokdefs` — `awk config cpp eqn grap hoc pic ratfor` — because the survey never
  runs `yacc`. A build order, not a defect.
- **Flags are generic, not per-unit.** `as` fails on `"as.h":217:unknown size` and
  `cc` on `"cc.c":42:BUFSIZE undefined`; both build under their own makefile's
  `-D`/`-I`. The survey passes one fixed set to all 356.

## What is genuinely a fact about the tape

The diagnostic histogram, which does not depend on my bucketing:

	23  syntax error
	11  Can't find include file y.tab.h
	11  Can't find include file stdlib.h
	 7  expected a NAME in list
	 7  expected , or ;
	 7  Can't find include file defs
	 5  n_un undefined / not struct-union member
	 3  Can't find include file stddef.h
	 3  Can't find include file /usr/jerq/include/jioctl.h
	 3  illegal pointer/integer combination, op =

- **`expected a NAME in list` is the prototype rejection** CLAUDE.md already
  records for V8's 1985 `cc`, appearing again here. With `syntax error` and
  `expected , or ;` that is the ANSI-versus-K&R wall, in the commands rather than
  in libc — the same two-generations split, one layer out.
- **`stdlib.h` and `stddef.h` are the variant-header question**, already answered
  once for libc by installing `CC/stdlib.h` and `lcc/float.h`: r70 keeps them only
  under `include/lcc/`, `include/CC/`, `include/olcc/` and `include/oCC/`. Seven
  more units want the same decision extended.
- **`n_un` is a.out.h skew** — `ld nm prof prof.old ranlib`, every one of them a
  program that reads object files. CLAUDE.md notes V10's `a.out.h` differs from
  V8's "by one bit-field *name* in the relocation record"; this is the `struct
  nlist` union under the same heading, and it is why these five cluster.
- **The 5620 headers cannot be reached by `-I` at all** for `dict`, `dis` and
  `mc`: they name `/usr/jerq/include/jioctl.h` as an ABSOLUTE path, so the tree
  has to be installed at that path rather than added to a search list. Same
  finding as `jterm.o` in libc, which cost two runs there.

## The survey was optimistic three times, and each time a control caught it

`tools/v10-world.py` predicts host-side which units can resolve every header.
Its own history is the useful part:

	348 of 351   a missing re.M anchored `^' to offset 0 of the whole file, so an
	             include was found only in a file whose FIRST character began one
	332 of 353   the cross-unit search ran BEFORE the system path, so ccom's
	             `#include "stdio.h"' resolved to cmd/lcc/include/sparc_sun/stdio.h
	318 of 356   the honest figure

Every error ran in the flattering direction, which is why each read as a result.
The first was caught because `ccom` appeared unbuildable and stage 3 proves it a
fixpoint; the second because a real compiler said `Can't find include file
config.h` for `asd`, whose directory has no `config.h`. A survey that cannot be
checked against something already known is not a survey.

Against the machine, the prediction is now good in one direction and weak in the
other: exactly **one** unit (`units`) compiled after being predicted blocked, so
the include model rarely says "impossible" when it is not; but 80 units cleared
the header hurdle and failed anyway, which is the language wall the survey says
outright it cannot see.

## Three harness faults, all of which cost a run

- **An unbounded log filled a 120 MB filesystem, twice, at the same unit.**
  `asd`'s `asd.h` includes a `config.h` that does not exist, and the compiler's
  output went to `>> u.log` with no limit. The transcript showed the message three
  times only because the DISPLAY was `sed -e 3q`. Then V10's allocator hid it:
  `lsys/fs/alloc.c` prints `file system full` and **sleeps** waiting for space
  that is not coming, so the run neither progressed nor died — a `vax750` at 100%
  CPU with a kernel message every few seconds. Fixed structurally, with
  `| sed -e 40q`: the pipe closes, the compiler gets EPIPE, and no counting or
  truncation can be outrun. The verdict still uses both tests, with the status
  carried through the pipe as its own line because a pipeline's status is `sed`'s
  and 1985 `sh` has no `PIPESTATUS`.
- **I blamed the image and was wrong.** The first failure was diagnosed as "the
  `.stage1.s2.s3` image's `/usr` is full because it carries four toolchain installs
  and two object trees", and the default was moved to `.stage1`. `tools/v10-free.py`
  then measured `/usr` at **111.8 MB free** the whole time. That tool exists because
  no guest-side probe can work here — a probe blocks in the same `alloc()` sleep as
  the thing it guards — and it counts the BITMAP rather than trusting `s_tfree`,
  which `alloc.c` sets to 0 on failure under Bell Labs' own commented-out line
  reading *"but it would be wrong FIX"*. Self-validating three ways: `struct filsys`
  computes to exactly one 4096-byte block, `s_fsmnt` reads `/mnt2` and `/mnt`, and
  the popcount matches `s_tfree`.
- **A guest script that exits without its end marker strands the driver.** The
  `dd` probe failed (`dd: not found` — the golden lacks it exactly as it lacks
  `ar`, `cmp`, `tail`, `grep` and `wc`), `worldc.sh` exited, and `v10_run` blocked
  for its full 18,000-second timeout with a simulator at 100% CPU and no console.
  CLAUDE.md's rule that a harness must terminate itself now has to hold inside the
  guest too: every top-level exit prints `WORLDC-done`.

And one piece of my own process worth writing down: **`bash tools/norun.sh` is not
a check.** It is a library to source, so running it defines two functions and exits
0 silently, which reads exactly like a pass. I used it as a check repeatedly. The
real guards were working throughout — `v10-srcdisk.sh` refused with *"norun: a
simulator is already running (pid13727)"* and `v10-compile.sh` refused on a stale
srcid — while I read a 42/42 out of the previous run's log file and believed a
rebuild had happened. No overlap occurred and nothing was corrupted. The guards
were right; the monitoring was sloppy.

## Next

1. **Compile from the overlay where one exists.** The cheapest real gain: it fixes
   `mv`, `fsck`, `cc`, `login` and `mkbitfs` immediately, and it makes the survey
   measure the tree we actually build rather than the tape alone.
2. **Run `yacc` before the units that need `y.tab.h`** — eight units, a build-order
   change rather than a porting one.
3. **Extend the variant-header decision** from libc's three to the seven more units
   asking for `stdlib.h` and `stddef.h`.
4. Then K10.2, the libraries: 17 `lib*` source trees plus `ipc/libipc` and
   `ipc/libin`, with `-lm` (41 uses), `-lipc` (35) and `-ll` (28) the most wanted.

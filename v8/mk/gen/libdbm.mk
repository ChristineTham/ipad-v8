# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libdbm.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libdbm, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: libdbm/Makefile: libdbm.a: dbm.o

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -I$(SRC)/usr/src/libdbm -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = dbm.o

all: libdbm.a

libdbm.a: $(OBJS) $(AR)
	-rm -f libdbm.a
	$(AR) cr libdbm.a $(OBJS)
	$(RANLIB) libdbm.a

dbm.o: $(SRC)/usr/src/libdbm/dbm.c $(INCDIR)/signal.h $(INCDIR)/sys/param.h $(INCDIR)/sys/stat.h $(INCDIR)/sys/types.h $(SRC)/usr/src/libdbm/dbm.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/libdbm/dbm.c

install: libdbm.a
	-mkdir $(DESTDIR)/usr/lib
	cp libdbm.a $(DESTDIR)/usr/lib/libdbm.a
	$(RANLIB) $(DESTDIR)/usr/lib/libdbm.a

clean:
	-rm -f $(OBJS) libdbm.a

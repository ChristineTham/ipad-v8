# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libl.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libl, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: lib/libl/makefile: ar rvc libl.a allprint main reject yyless yywrap -- the tape compiles yywrap separately, which changes nothing since ar is given the order explicitly

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = allprint.o \
	main.o \
	reject.o \
	yyless.o \
	yywrap.o

all: libl.a

libl.a: $(OBJS) $(AR)
	-rm -f libl.a
	$(AR) cr libl.a $(OBJS)
	$(RANLIB) libl.a

allprint.o: $(SRC)/usr/src/lib/libl/allprint.c $(INCDIR)/stdio.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libl/allprint.c

main.o: $(SRC)/usr/src/lib/libl/main.c $(INCDIR)/stdio.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libl/main.c

reject.o: $(SRC)/usr/src/lib/libl/reject.c $(INCDIR)/stdio.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libl/reject.c

yyless.o: $(SRC)/usr/src/lib/libl/yyless.c $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libl/yyless.c

yywrap.o: $(SRC)/usr/src/lib/libl/yywrap.c $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libl/yywrap.c

install: libl.a
	-mkdir $(DESTDIR)/usr/lib
	cp libl.a $(DESTDIR)/usr/lib/libl.a
	$(RANLIB) $(DESTDIR)/usr/lib/libl.a

clean:
	-rm -f $(OBJS) libl.a

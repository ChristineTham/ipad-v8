# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
# Run from the component's source directory:
#     make -f $SRC/mk/gen/config.mk TOOLDIR=... install
#
# Every object below depends on the compiler passes that produced it, so
# replacing $(CCOM) rebuilds all of them; every binary depends on $(LD) and
# $(LIBC). That is the invalidation a self-hosting build needs and V8's make
# cannot work out for itself.

# Source is READ ONLY and lives on the netfs share.  Nothing is ever copied to
# local disk: objects are written into the current directory, which the driver
# makes a per-component directory on the build filesystem.  That is what the
# share is for, and it is why $(SRC) appears on every source path below.
SRC     = /n/src
TOOLDIR = /b/tools
DESTDIR = /b/root

# Stage 0 is the running system; later stages override these to point -B at
# the tools they just built.  Nothing here ever writes outside
# $(TOOLDIR)/$(DESTDIR).
#
# Each of cc, yacc and lex needs TWO macros, and they are not interchangeable.
# $(CC) is what a rule runs -- a command, resolved on PATH, and in later
# stages a whole phrase with -B and -t in it.  $(CCPATH) is the file whose
# timestamp means "the compiler changed", and that is what a prerequisite list
# needs.  Writing "CC = cc" and then using $(CC) as a prerequisite produced
#
#	Make:  Don't know how to make cc.  Stop.
#
# on all fourteen components at once: make went looking for a file named cc in
# the build directory.  The other tools are already absolute paths, so they
# serve as both.
CC       = cc
CCPATH   = /bin/cc
YACC     = yacc
YACCPATH = /usr/bin/yacc
LEX      = lex
LEXPATH  = /usr/bin/lex
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
LIBC = /lib/libc.a

# Where <angle-bracket> headers really come from.  Stage 1 builds against the
# running system, like any bootstrap; stage 5 onward points this at
# $(DESTDIR)/usr/include so touching our headers rebuilds what includes them.
INCDIR = $(SRC)/usr/include

CFLAGS = -O 
INCS   = -I. -I$(SRC)/usr/src/cmd/config -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) $(INCS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = lex.yy.o main.o mkconf.o mkheaders.o mkioconf.o mkmakefile.o mkubglue.o y.tab.o

all: config

config: $(OBJS) $(LD) $(LIBC) $(DESTDIR)/usr/lib/libl.a
	$(CC) $(CFLAGS) -o config $(OBJS) $(DESTDIR)/usr/lib/libl.a $(LIBC)

lex.yy.c: $(SRC)/usr/src/cmd/config/config.l $(LEXPATH) $(INCDIR)/ctype.h $(SRC)/usr/src/cmd/config/config.h
	$(LEX) $(SRC)/usr/src/cmd/config/config.l

y.tab.c: $(SRC)/usr/src/cmd/config/config.y $(YACCPATH) $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h
	$(YACC) -d $(SRC)/usr/src/cmd/config/config.y

# written by the y.tab.c rule above
y.tab.h: y.tab.c
	@echo y.tab.h

lex.yy.o: lex.yy.c y.tab.h $(TOOLS)
	$(COMPILE) lex.yy.c

main.o: $(SRC)/usr/src/cmd/config/main.c $(INCDIR)/ctype.h $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/main.c

mkconf.o: $(SRC)/usr/src/cmd/config/mkconf.c $(INCDIR)/ctype.h $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/mkconf.c

mkheaders.o: $(SRC)/usr/src/cmd/config/mkheaders.c $(INCDIR)/ctype.h $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/mkheaders.c

mkioconf.o: $(SRC)/usr/src/cmd/config/mkioconf.c $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/mkioconf.c

mkmakefile.o: $(SRC)/usr/src/cmd/config/mkmakefile.c $(INCDIR)/ctype.h $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/mkmakefile.c

mkubglue.o: $(SRC)/usr/src/cmd/config/mkubglue.c $(INCDIR)/stdio.h $(SRC)/usr/src/cmd/config/config.h y.tab.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/cmd/config/mkubglue.c

y.tab.o: y.tab.c y.tab.h $(TOOLS)
	$(COMPILE) y.tab.c

install: config
	-mkdir $(DESTDIR)/etc
	cp config $(DESTDIR)/etc/config

clean:
	-rm -f $(OBJS) config lex.yy.c y.tab.c

# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libg.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libg, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: lib/libg/makefile: as dbxxx.s -o libg.a

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -I$(SRC)/usr/src/lib/libg -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)


all: libg.a

libg.a: $(SRC)/usr/src/lib/libg/dbxxx.s $(AS)
	$(AS) $(SRC)/usr/src/lib/libg/dbxxx.s -o libg.a

install: libg.a
	-mkdir $(DESTDIR)/usr/lib
	cp libg.a $(DESTDIR)/usr/lib/libg.a

clean:
	-rm -f libg.a

# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libtermlib.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libtermlib, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: lib/libtermlib/makefile: builds termcap.a from three of its six .c (tc1/tc2/tc3 are tests), installs as libtermcap.a and hard-links libtermlib.a to it -- so -ltermlib and -ltermcap are one file

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -DCM_N -DCM_GT -DCM_B -DCM_D -I$(SRC)/usr/src/lib/libtermlib -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = termcap.o \
	tgoto.o \
	tputs.o

all: libtermcap.a

libtermcap.a: $(OBJS) $(AR)
	-rm -f libtermcap.a
	$(AR) cr libtermcap.a $(OBJS)
	$(RANLIB) libtermcap.a

termcap.o: $(SRC)/usr/src/lib/libtermlib/termcap.c $(INCDIR)/ctype.h $(INCDIR)/local/uparm.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libtermlib/termcap.c

tgoto.o: $(SRC)/usr/src/lib/libtermlib/tgoto.c $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libtermlib/tgoto.c

tputs.o: $(SRC)/usr/src/lib/libtermlib/tputs.c $(INCDIR)/ctype.h $(INCDIR)/sgtty.h $(INCDIR)/signal.h $(INCDIR)/sys/ioctl.h $(INCDIR)/sys/param.h $(INCDIR)/sys/types.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libtermlib/tputs.c

install: libtermcap.a
	-mkdir $(DESTDIR)/usr/lib
	cp libtermcap.a $(DESTDIR)/usr/lib/libtermcap.a
	$(RANLIB) $(DESTDIR)/usr/lib/libtermcap.a
	-rm -f $(DESTDIR)/usr/lib/libtermlib.a
	ln $(DESTDIR)/usr/lib/libtermcap.a $(DESTDIR)/usr/lib/libtermlib.a

clean:
	-rm -f $(OBJS) libtermcap.a

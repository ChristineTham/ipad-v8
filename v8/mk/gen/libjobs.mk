# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libjobs.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libjobs, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: lib/libjobs/makefile: OBJS -- two .c and four .s

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -I$(SRC)/usr/src/lib/libjobs -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = getwd.o \
	killpg.o \
	setpgrp.o \
	signal.o \
	sigset.o \
	wait3.o

all: libjobs.a

libjobs.a: $(OBJS) $(AR)
	-rm -f libjobs.a
	$(AR) cr libjobs.a $(OBJS)
	$(RANLIB) libjobs.a

getwd.o: $(SRC)/usr/src/lib/libjobs/getwd.c $(INCDIR)/signal.h $(INCDIR)/sys/dir.h $(INCDIR)/sys/param.h $(INCDIR)/sys/stat.h $(INCDIR)/sys/types.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/getwd.c

killpg.o: $(SRC)/usr/src/lib/libjobs/killpg.s $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/killpg.s

setpgrp.o: $(SRC)/usr/src/lib/libjobs/setpgrp.s $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/setpgrp.s

signal.o: $(SRC)/usr/src/lib/libjobs/signal.s $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/signal.s

sigset.o: $(SRC)/usr/src/lib/libjobs/sigset.c $(INCDIR)/errno.h $(INCDIR)/signal.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/sigset.c

wait3.o: $(SRC)/usr/src/lib/libjobs/wait3.s $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/lib/libjobs/wait3.s

install: libjobs.a
	-mkdir $(DESTDIR)/usr/lib
	cp libjobs.a $(DESTDIR)/usr/lib/libjobs.a
	$(RANLIB) $(DESTDIR)/usr/lib/libjobs.a

clean:
	-rm -f $(OBJS) libjobs.a

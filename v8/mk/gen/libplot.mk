# Generated by v8/mk/mkdep.py -- do not edit; edit the generator.
#
#     make -f $SRC/mk/gen/libplot.mk TOOLDIR=... DESTDIR=... install
#
# Stage 5: libplot, built with the stage-3 toolchain against the headers
# stage 4 installed.  Source is read-only on the share; objects land in the
# current directory, which the driver makes per-library on the build disk.
#
# From the tape: libplot/libplot/makefile: cp plot.c.a/* . ; cc -cO *.c ; ar rc libplot.a *.o

SRC     = /n/src
TOOLDIR = /b/tools3
DESTDIR = /b/root

CC       = cc
CCPATH   = /bin/cc
CPP  = /lib/cpp
CCOM = /lib/ccom
C2   = /lib/c2
AS   = /bin/as
LD   = /bin/ld
AR   = /bin/ar
RANLIB = /usr/bin/ranlib

# Stage 4's headers, not the running system's.  This is the whole reason
# stage 4 is a stage.
INCDIR = $(DESTDIR)/usr/include

CFLAGS = -O -I$(INCDIR)
COMPILE = $(CC) $(CFLAGS) -c
TOOLS  = $(CCPATH) $(CCOM) $(CPP) $(C2) $(AS)

OBJS = subr.o \
	whoami.o

all: libplot.a

libplot.a: $(OBJS) $(AR)
	-rm -f libplot.a
	$(AR) cr libplot.a $(OBJS)
	$(RANLIB) libplot.a

subr.o: $(SRC)/usr/src/libplot/libplot/plot.c.a/subr.c $(INCDIR)/stdio.h $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/libplot/libplot/plot.c.a/subr.c

whoami.o: $(SRC)/usr/src/libplot/libplot/plot.c.a/whoami.c $(TOOLS)
	$(COMPILE) $(SRC)/usr/src/libplot/libplot/plot.c.a/whoami.c

install: libplot.a
	-mkdir $(DESTDIR)/usr/lib
	cp libplot.a $(DESTDIR)/usr/lib/libplot.a
	$(RANLIB) $(DESTDIR)/usr/lib/libplot.a

clean:
	-rm -f $(OBJS) libplot.a
